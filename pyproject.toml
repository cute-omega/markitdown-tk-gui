[project]
dependencies = [
  "markitdown[all]>=0.1.5",
]
description = "A tkinter GUI for markitdown, a Markdown to HTML converter."
name = "markitdown-tk-gui"
readme = "README.md"
requires-python = ">=3.12"
version = "1.0.0"
